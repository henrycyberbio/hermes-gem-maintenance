# Request

Please add acetate kinase to the model. It converts acetate and ATP into
acetyl-phosphate and ADP, all in the cytosol.

Use `ACKr` as the reaction identifier — that's the standard BiGG name for it. Make it
reversible, bounds -1000 to 1000. Gene rule `ackA`.

The model is in `examples/add-reaction/model/`.
